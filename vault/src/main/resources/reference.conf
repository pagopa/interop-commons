### This reference file tracks the expected configuration format for the vault manager ###

# interop-commons {
#   vault {
#     address = ${VAULT_ADDR}
#     token = ${VAULT_TOKEN}
#     }
#   }


interop-commons {
  vault {
    sslEnabled = true
  }
}